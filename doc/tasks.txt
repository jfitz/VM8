resources:
  http://www.gaby.de/cpm/manuals/archive/cpm22htm/index.htm
  https://jefftranter.blogspot.com/
  https://cpm8680.com/cpmc64/index.htm
  https://en.wikipedia.org/wiki/Intel_HEX

asm:
 options:
 processing:
   parse lines and allow for string literals (single quotes)
 directives:
   .byte
   .string
   .stringz
   .nstring
   .mnstring
   .mstringz
 output:
   multiple bytes on one line (8 for octal, 16 for hex)
 tests:
 
linker:
 options:
 input:
   read multiple bytes on line
 output:
   multiple bytes on one line (8 for octal, 16 for hex)
 test
   for octal output
   for hex output

dasm:
 options:
 input:
   read multiple bytes on line
 output:
   in hex or octal (--hex, --octal, default octal, cannot have both)
   multiple bytes on one line (8 for octal, 16 for hex)
 test
   for octal output
   for hex output

loader:
 options:
 input:
   read multiple bytes on line
 output:
   multiple bytes on one line (8 for octal, 16 for hex)
 test
   for octal output
   for hex output

binner:
 purpose:
   convert 'positioned' output to binary
 input:
   check format of input line
   read multiple bytes on line
 processing:
   convert executable section
   convert readonly section
   convert writable section
 output:
 tests:
   requires post-processing to convert binary to text, for comparison

